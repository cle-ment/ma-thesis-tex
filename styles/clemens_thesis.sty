%% Use this if you write hard core mathematics, these are usually needed
\usepackage{amsfonts,amssymb,amsbsy}

%% Use the macros in this package to change how the hyperref package below
%% typesets its hypertext -- hyperlink colour, font, etc. See the package
%% documentation. It also defines the \url macro, so use the package when
%% not using the hyperref package.
%%
%\usepackage{url}

%% Use this if you want to get links and nice output. Works well with pdflatex.
\PassOptionsToPackage{hyphens}{url}\usepackage{hyperref}
\hypersetup{pdfpagemode=UseNone, pdfstartview=FitH,
  colorlinks=true,
  urlcolor=[HTML]{191970},
  linkcolor=[HTML]{191970},
  citecolor=[HTML]{191970},
  % urlcolor=[HTML]{000060},
  % linkcolor=[HTML]{000060},
  % citecolor=[HTML]{000060},
  pdftitle={Default Title, Modify},
  pdfauthor={Clemens Westrup},
  pdfkeywords={Modify keywords}}

\usepackage{tabularx}
\usepackage{minted}

\usepackage{amsmath}
\usepackage[english]{babel}
\usepackage[autostyle]{csquotes}
\usepackage{caption}
\usepackage{subcaption}
\usepackage{todonotes}
\usepackage{pdfpages}
\usepackage{wrapfig}
\usepackage{multirow}

\usepackage{booktabs}
\setlength{\heavyrulewidth}{1.5pt}
\setlength{\abovetopsep}{4pt}

\setboolean{@twoside}{false}


% Glossaries
\usepackage[nopostdot,acronym,toc,nogroupskip]{glossaries}
\makeglossaries
% reset entries at the start of each chapter
\preto\section{\glsresetall}

% \usepackage{xparse}
% \DeclareDocumentCommand{\newdualentry}{ O{} O{} m m m m } {
%   \newglossaryentry{gls-#3}{name={#5},text={#5\glsadd{#3}},
%     description={#6},#1
%   }
%   \newacronym[see={[Glossary:]{gls-#3}},#2]{#3}{#4}{#5\glsadd{gls-#3}}
% }

\usepackage{xparse}
\DeclareDocumentCommand{\newdualentry}{ O{} O{} m m m m } {
  \newglossaryentry{#5}{name={#5},text={#5},
    description={#6},#1
  }
  \newacronym[#2]{#3}{#4}{\gls{#5}}
}

% Define argmin and argmax operators
\newcommand*{\argmin}{\operatornamewithlimits{argmin}\limits}
\newcommand*{\argmax}{\operatornamewithlimits{argmax}\limits}

% Math bold italic letters:
\def\mathbi#1{\textbf{\em #1}}
